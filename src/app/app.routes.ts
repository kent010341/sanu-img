import { Routes } from '@angular/router';
import { EditorWorkspace } from './layout/editor-workspace/editor-workspace';

export const routes: Routes = [
    { path: '', component: EditorWorkspace }
];
